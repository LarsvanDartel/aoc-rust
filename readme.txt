To use:
1. Get session key
2. Set the session key as an environment variable
3. You can either run 'cargo scaffold' to generate a template for a new day and autimatically download the input file, or you can run 'cargo solve' to run the code for the current day.